import 'package:flutter/material.dart';

/// App Color Palette
/// Design System: Horizon Attendance
class AppColors {
  AppColors._();

  // Primary Colors
  static const Color primary = Color(0xFF0A2A66); // Dark Blue
  static const Color primaryLight = Color(0xFF1A3A76);
  static const Color primaryDark = Color(0xFF051A46);

  // Secondary Colors
  static const Color secondary = Color(0xFFF5F6FA); // Light Gray
  static const Color secondaryLight = Color(0xFFFFFFFF);
  static const Color secondaryDark = Color(0xFFE5E6EA);

  // Background Colors
  static const Color background = Color(0xFFFFFFFF); // White
  static const Color surface = Color(0xFFFFFFFF);
  static const Color surfaceVariant = Color(0xFFF5F6FA);

  // Text Colors
  static const Color textPrimary = Color(0xFF0A2A66);
  static const Color textSecondary = Color(0xFF6B7280);
  static const Color textTertiary = Color(0xFF9CA3AF);
  static const Color textOnPrimary = Color(0xFFFFFFFF);

  // Utility Colors
  static const Color success = Color(0xFF10B981);
  static const Color error = Color(0xFFEF4444);
  static const Color warning = Color(0xFFF59E0B);
  static const Color info = Color(0xFF3B82F6);

  // Border Colors
  static const Color border = Color(0xFFE5E7EB);
  static const Color borderLight = Color(0xFFF3F4F6);

  // Shadow Colors
  static const Color shadow = Color(0x0F000000); // Minimal shadow (6% opacity)
  static const Color shadowLight = Color(0x08000000); // Very light shadow (3% opacity)
}
